[package]
name = "lattice-types"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <your.email@example.com>"]
description = "Core types and traits for the Lattice search engine"
license = "MIT OR Apache-2.0"
repository = "https://github.com/yourusername/lattice"

[dependencies]
